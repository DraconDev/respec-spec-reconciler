{
  "name": "respec",
  "version": "0.1.1",
  "type": "module",
  "description": "Continuous spec-driven reconciliation for Pi",
  "license": "MIT",
  "author": "Dracon",
  "keywords": [
    "spec-reconciler",
    "spec",
    "reconciliation",
    "invariant",
    "verifier",
    "pi-extension",
    "ai-agent"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DraconDev/respec-spec-reconciler.git",
    "directory": "."
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "scripts": {
    "dev": "pi -e ./src/index.ts",
    "build": "tsc",
    "typecheck": "npx tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.7",
    "vitest": "^4.1.7"
  }
}
